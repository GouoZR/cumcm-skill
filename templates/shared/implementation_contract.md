# 实现契约

## 输入与数据口径

## 环境、依赖与复现命令

## 输出文件、字段与版本标识

核心数字必须登记到 `results/result_registry.json`，正文、表格和图片不直接依赖人工抄写的临时值。

## 规格—代码—结果映射

| 规格/公式 ID | 模块或函数 | 参数/约束 | 结果文件或图表 | 验收测试 |
|---|---|---|---|---|

## 模块划分

正式求解/预测函数与审计评估器尽量分离；独立复算不得只是再次调用正式模型的总目标函数。

## 算法、参数与随机种子

## 必须通过的不变量与反例测试

逐条对应 `artifacts/quality_contract.json` 中的 invariants，并记录测试命令、预期关系和证据文件。

## 基线、独立复算与 sanity check

按题型选择朴素基线、小规模精确解、替代求解器、留出集、守恒关系、文献量级或人工小算例。若没有合理基线，说明原因并给出替代证据。

## 保真度与离散化检查

## 指标分解与约束诊断

优化/评价/聚合问题应输出必要的组成项、归一化前后值、重复或冲突损失、约束违反量及各对象贡献；预测/分类问题应输出分组误差或混淆信息；仿真问题应输出步长、网格或重复试验诊断。

## 图表、结果表与证据链要求

每个 primary 指标必须能从结果注册表定位到原始结果文件，并绑定至少一个表、图、日志或独立复算证据。

## 允许的实现调整

任何影响模型定义、目标函数、约束、指标、聚合口径、数据范围、离散化或结论作用域的调整，必须记录到 `artifacts/model_deviations.md`，同步更新 `artifacts/quality_contract.json`，重新计算哈希并重新运行；不得静默替换。
